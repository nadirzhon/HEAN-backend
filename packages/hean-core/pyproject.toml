[project]
name = "hean-core"
version = "2.0.0"
description = "HEAN Core — EventBus, types, config, regime detection"
requires-python = ">=3.11"
dependencies = [
    "pydantic>=2.0.0",
    "pydantic-settings>=2.0.0",
    "aiohttp>=3.9.0",
    "redis>=5.0.0",
    "orjson>=3.9.0",
    "python-dotenv>=1.0.0",
    "numpy>=1.24.0",
    "networkx>=3.2.0",
    "psutil>=5.9.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/hean"]
