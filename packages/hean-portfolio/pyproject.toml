[project]
name = "hean-portfolio"
version = "2.0.0"
description = "HEAN Portfolio — Accounting, capital allocation, profit capture"
requires-python = ">=3.11"
dependencies = [
    "hean-core",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/hean"]
