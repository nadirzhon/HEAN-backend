[project]
name = "hean-physics"
version = "2.0.0"
description = "HEAN Physics — Market thermodynamics, entropy, phase detection, Szilard engine"
requires-python = ">=3.11"
dependencies = [
    "hean-core",
    "numpy>=1.24.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/hean"]
