[project]
name = "hean-exchange"
version = "2.0.0"
description = "HEAN Exchange — Bybit HTTP/WebSocket clients"
requires-python = ">=3.11"
dependencies = [
    "hean-core",
    "aiohttp>=3.9.0",
    "websockets>=12.0",
    "orjson>=3.9.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/hean"]
