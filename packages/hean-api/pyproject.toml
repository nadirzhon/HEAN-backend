[project]
name = "hean-api"
version = "2.0.0"
description = "HEAN API — FastAPI gateway with REST and WebSocket"
requires-python = ">=3.11"
dependencies = [
    "hean-core",
    "hean-exchange",
    "hean-portfolio",
    "hean-risk",
    "hean-execution",
    "hean-strategies",
    "hean-physics",
    "hean-intelligence",
    "hean-observability",
    "fastapi>=0.104.0",
    "uvicorn[standard]>=0.24.0",
    "slowapi>=0.1.9",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/hean"]
