[project]
name = "hean-intelligence"
version = "2.0.0"
description = "HEAN Intelligence — Brain (Claude AI), Council, Sentiment, Archon"
requires-python = ">=3.11"
dependencies = [
    "hean-core",
    "aiohttp>=3.9.0",
    "httpx>=0.25.0",
]

[project.optional-dependencies]
sentiment = [
    "transformers>=4.30.0",
    "torch>=2.0.0",
    "sentencepiece>=0.1.99",
    "tweepy>=4.14.0",
    "praw>=7.7.0",
    "beautifulsoup4>=4.12.0",
    "feedparser>=6.0.10",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/hean"]
