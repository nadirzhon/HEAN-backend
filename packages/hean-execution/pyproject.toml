[project]
name = "hean-execution"
version = "2.0.0"
description = "HEAN Execution — Order routing, position reconciliation, idempotency"
requires-python = ">=3.11"
dependencies = [
    "hean-core",
    "hean-exchange",
    "hean-portfolio",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/hean"]
