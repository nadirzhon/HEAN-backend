[project]
name = "hean-risk"
version = "2.0.0"
description = "HEAN Risk — RiskGovernor, KillSwitch, position sizing, Kelly criterion"
requires-python = ">=3.11"
dependencies = [
    "hean-core",
    "hean-portfolio",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/hean"]
