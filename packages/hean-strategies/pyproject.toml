[project]
name = "hean-strategies"
version = "2.0.0"
description = "HEAN Strategies — ImpulseEngine, FundingHarvester, and 9 more trading strategies"
requires-python = ">=3.11"
dependencies = [
    "hean-core",
    "hean-execution",
    "hean-risk",
    "hean-observability",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/hean"]
